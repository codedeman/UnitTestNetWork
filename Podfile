# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestDesignPattern' do
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'
  pod 'Alamofire', '~> 5.0.0-rc.3'
  
  target 'TestDesignPatternTests' do
    inherit! :search_paths
  end

end
